extends layout

block content
  form(action="/schedules", method="post").my-3
    input(type="hidden",name="_csrf",value!=csrfToken)
    div.form-group
      label(for="scheduleName") 予定名
      input(type="text", name="scheduleName" maxlength="255")#scheduleName.form-control
    div.form-group
      label(for="memo") memo(500文字まで)
      textarea(name="memo")#memo.form-control
    div.form-group
      label(for="scheduleName") 候補日程 (改行して複数入力してください)
      textarea(name="candidates")#candidates.form-control
    button(type="submit").btn.btn-info 予定作成